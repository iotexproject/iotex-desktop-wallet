// @ts-ignore
import { styled } from "onefx/lib/styletron-react";

export const CommonMargin = styled("div", (_: React.CSSProperties) => ({
  margin: "16px"
}));

export const SmallMargin = styled("div", (_: React.CSSProperties) => ({
  margin: "8px"
}));
